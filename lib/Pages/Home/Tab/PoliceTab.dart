import 'package:flutter/material.dart';

class PoliceTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Police Screen Content"),
    );
  }
}