

import 'package:flutter/material.dart';

class HospitalTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hospital Screen Content"),
    );
  }
}